;redcode
;assert 1
	MOV 5184, <-74
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -1, -2712
	JMP -57, -6747
	JMP -57, -6747
	JMP -1, -2712
	JMP -4001, #-5312
	SUB -6321, 8572
	JMP -1, -2712
	JMP -1, -2712
