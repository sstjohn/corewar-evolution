;redcode
;assert 1
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	ADD -201, @-2038
	JMP -1, #2038
	JMN 5, #5038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
