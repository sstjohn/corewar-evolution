;redcode
;assert 1
	MOV 8898, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV -898, <-90
	MOV -898, <-90
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -1, 2593
	JMP -1, 2593
	JMP -1, -2793
	JMP -1, -2093
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMN -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
	MOV 8898, <-94
	JMP -1, -2093
	JMP -91, -2099
	JMP -1, -2093
