;redcode
;assert 1
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	ADD -206, @-2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	MOV 9427, <-94
	MOV 9427, <-94
	MOV 9427, <-94
	ADD -206, @-2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	ADD -206, @-2038
	JMP -1, #2038
	ADD -206, @-2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	MOV 9427, <-94
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
	JMP -1, #2038
