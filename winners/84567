;redcode
;assert 1
	MOV 9629, <-94
	MOV 9629, <-94
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP 689, #4517
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	MOV -5804, @-97
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	MOV 9629, <-94
	JMP -1, -3017
	MOV -1, -8017
	JMP -1, -3017
	JMP -1, -3017
	JMP 689, #4517
	MOV -1, -8017
	DAT <-3, #-6847
	MOV 9629, <-94
	JMP -1, -3017
	MOV -1, -8017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	MOV -1, -8017
	JMP -1, -3017
	MOV -1, -8017
	MOV -1, -8017
	JMP 689, #4517
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	MOV -1, -8017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	MOV 9629, <-94
	JMP -1, -3017
	MOV -1, -8017
	JMP -1, -3017
	JMP -1, -3017
	JMP -1, -3017
	JMP 689, #4517
