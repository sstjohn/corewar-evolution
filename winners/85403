;redcode
;assert 1
	MOV 456, <-55
	JMP -1, #2491
	JMP -1, #2491
	JMP -1, #2491
	JMN 1456, @4051
	JMP -1, #2491
	JMP -1, #2491
	JMN 1456, @4051
	JMP -1, #2491
	JMP -1, #2491
	MOV 456, <-55
	JMN 1456, @4051
	JMN 1456, @4051
	MOV 456, <-55
	JMP -1, #2491
