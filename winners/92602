;redcode
;assert 1
	MOV 8828, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMN -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -71, -753
	MOV 8828, <-94
	JMP -1, -2093
	JMP -1, -2093
	JMP -1, -2093
	JMP -71, -753
	MOV 8828, <-94
	MOV 8828, <-94
	JMP -1, -2093
	JMP -1, -2093
	DJN -4005, 2018
	JMP -1, -2093
	JMP -1, -2093
	JMP -71, -753
	JMP -71, -753
	JMP -1, -2093
	JMP -71, -753
	MOV 8828, <-94
	DAT #0, #-2093
	MOV 8828, <-94
	JMN -1, -2093
	JMP -1, -2093
	JMP -1, -2093
