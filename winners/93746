;redcode
;assert 1
	MOV 503, <-8090
	JMP -1, 2077
	JMP -2, -62
	JMP -1, 2077
	JMP -901, @2011
	JMP -2, -62
	JMP @-971, 2077
	JMP -1, 2077
	JMP -901, @2011
	JMP -901, @2011
	JMP -901, @2011
	JMP -901, @2011
	JMP -901, @2011
